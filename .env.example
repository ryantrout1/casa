# Neon connection string for the casa_rewards database.
# Add this in Vercel: Project Settings → Environment Variables → DATABASE_URL
# (copy the pooled connection string from the Neon console for casa_rewards).
DATABASE_URL="postgresql://USER:PASSWORD@HOST/casa_rewards?sslmode=require"
